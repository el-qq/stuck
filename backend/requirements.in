# Direct runtime dependencies. Exact direct/transitive versions are generated
# into requirements.txt by `npm run deps:update`.
fastapi
starlette
uvicorn[standard]
httpx
# NGFW second-factor (2FA) challenge is a WebSocket the backend proxies.
websockets
pydantic
pydantic-settings
